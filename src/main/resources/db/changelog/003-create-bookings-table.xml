<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.24.xsd">

    <changeSet id="3" author="midas">
        <createTable tableName="bookings">
            <column name="id" type="INTEGER" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="display_id" type="INTEGER"/>
            <column name="shareholder_id" type="INTEGER">
                <constraints nullable="false" foreignKeyName="fk_booking_shareholder" referencedTableName="shareholders" referencedColumnNames="id"/>
            </column>
            <column name="date" type="DATE">
                <constraints nullable="false"/>
            </column>
            <column name="type" type="INTEGER">
                <constraints nullable="false"/>
            </column>
            <column name="amount" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="comment" type="TEXT"/>
        </createTable>

        <sql dbms="sqlite">
            CREATE TRIGGER set_booking_display_id
            AFTER INSERT ON bookings
            FOR EACH ROW
            WHEN (NEW.display_id IS NULL OR NEW.display_id = 0)
            BEGIN
                UPDATE bookings SET display_id = NEW.id WHERE id = NEW.id;
            END;
        </sql>
    </changeSet>

</databaseChangeLog>
